{
  "name": "fela-test",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && npm start",
    "server": "nodemon server.js",
    "dev": "cd components && npm run build && cd .. && concurrently --kill-others-on-fail \"node server\" \"cd client/ && npm start\""
  },
  "dependencies": {
    "express": "^4.16.4"
  },
  "devDependencies": {
    "concurrently": "^4.0.1"
  }
}
