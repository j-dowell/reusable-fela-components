{
  "name": "fela-test",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && npm start",
    "server": "nodemon server.js",
    "dev": "cd components && npm run build && cd ../ && npm i && concurrently --kill-others-on-fail \"node server\" \"cd client/ && npm i && npm start\""
  },
  "dependencies": {
    "express": "^4.16.4"
  },
  "devDependencies": {
    "concurrently": "^4.0.1"
  }
}
